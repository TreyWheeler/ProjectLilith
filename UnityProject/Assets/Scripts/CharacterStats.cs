using UnityEngine;
using System.Collections;

public class CharacterStats : MonoBehaviour {

	// Use this for initialization
	void Start () {
        GUITexture face1 = new GUITexture();
        face1.pixelInset = new Rect(0, 0, 0, 0);
        face1.transform.position = new Vector3(0,1,0);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
